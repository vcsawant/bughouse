#!/bin/sh
set -eu

cd -P -- "$(dirname -- "$0")"

# Run migrations before starting (safe to run on every deploy — no-ops if up to date)
./bughouse eval Bughouse.Release.migrate

PHX_SERVER=true exec ./bughouse start
